تمام ✅ خلينا نركز فقط على **الأوامر والخيارات اللي تتحكم في قوة Stockfish 17.1** عبر **UCI protocol**.

Stockfish محرك قوي جدًا (أكثر من 3500 Elo)، لكن تقدر تضعف مستواه بطريقتين:

1. التحكم في **Skill Level** (0 → 20).
2. التحكم في **Elo rating وهمي** عبر `UCI_LimitStrength` و `UCI_Elo`.

---

## 🔹 أوامر التحكم في قوة Stockfish

### 1. Skill Level

* المدى: **0 (أضعف) → 20 (أقوى)**.
* يؤثر على اختيار النقلات (يلعب أحيانًا حركات أضعف عند مستوى منخفض).

📌 الأمر:

```
setoption name Skill Level value 5
```

* `0` = يلعب كهاوي ضعيف.
* `10` = مستوى متوسط.
* `20` = القوة الكاملة.

---

### 2. UCI\_LimitStrength + UCI\_Elo

* إذا أردت أن تحدد قوة المحرك بمقياس **Elo rating**.
* لازم أولًا تفعل `UCI_LimitStrength`، ثم تحدد قيمة `UCI_Elo`.

📌 الأوامر:

```
setoption name UCI_LimitStrength value true
setoption name UCI_Elo value 1200
```

* المدى: **1320 → 3190 Elo** (يعتمد على نسخة Stockfish).
* مثال:

  * `UCI_Elo 1350` = يلعب بمستوى مبتدئ.
  * `UCI_Elo 2000` = مستوى لاعب نادي.
  * `UCI_Elo 2800` = مستوى أبطال العالم.

---

### 3. Threads

* عدد الـ CPU threads المستعملة.
* تقليلها ممكن يقلل القوة قليلًا.

📌 الأمر:

```
setoption name Threads value 1
```

* `1` = أضعف (يستخدم نواة واحدة فقط).
* `4` أو أكثر = أقوى (يستفيد من تعدد الأنوية).

---

### 4. Hash

* حجم ذاكرة الـ Hash Table (بالـ MB).
* تقليلها → يقلل قوة الحساب.

📌 الأمر:

```
setoption name Hash value 16
```

* `16 MB` = أضعف.
* `1024 MB` أو أكثر = أقوى.

---

### 5. Contempt

* خيار يحدد “درجة عداء” المحرك للتعادل.
* لا يؤثر بشكل مباشر على القوة، لكن يغير أسلوب اللعب.

📌 الأمر:

```
setoption name Contempt value 0
```

* `0` = محايد.
* قيمة موجبة = يفضل الفوز على التعادل.
* قيمة سالبة = يقبل التعادلات بسهولة.

---

## 🔹 أمثلة عملية

### مثال 1: محرك ضعيف (مبتدئ)

```uci
setoption name Skill Level value 0
setoption name UCI_LimitStrength value true
setoption name UCI_Elo value 1200
setoption name Threads value 1
setoption name Hash value 16
```

---

### مثال 2: محرك متوسط

```uci
setoption name Skill Level value 10
setoption name UCI_LimitStrength value true
setoption name UCI_Elo value 1800
setoption name Threads value 2
setoption name Hash value 128
```

---

### مثال 3: محرك بأقصى قوة

```uci
setoption name Skill Level value 20
setoption name UCI_LimitStrength value false
setoption name Threads value 8
setoption name Hash value 1024
```

---

## 📌 الخلاصة

الأوامر الأساسية للتحكم في قوة المحرك:

* `setoption name Skill Level value [0–20]`
* `setoption name UCI_LimitStrength value true/false`
* `setoption name UCI_Elo value [1320–3190]`
* `setoption name Threads value [1–N]`
* `setoption name Hash value [16–N]`

---

تحب ألخص لك هذه الأوامر في **جدول مختصر (Command + وصف + مثال)** بحيث تستعمله كمرجع سريع؟
